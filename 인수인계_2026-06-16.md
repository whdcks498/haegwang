# 인수인계 — 2026-06-16 (집 PC 해광AI비서 → 회사 PC 코워크)

> 회사 코워크 읽는 법: 이 저장소를 `git pull`로 최신화한 뒤 이 문서를 읽고 메모리에 반영. 오늘 홈페이지에 큰 변경이 많았으니 꼭 숙지. **해광툰(웹툰)은 대표님 직속·집 PC 전용이라 인수인계 대상 아님.**

## 오늘(6/16) 추가·변경된 것 (전부 배포·라이브 검증 완료)

### 1. 방문자 통계 — 구글 애널리틱스(GA4) 신규 설치
- 측정 ID **G-SGNM9VX949** (gtag.js 스니펫을 **전 32개 페이지 `<head>`**에 삽입).
- GA 속성 이름은 "naver.me"(기존 재사용), 그 안의 **"화물구인구직" 스트림**이 우리 것. 보는 법: analytics.google.com → 보고서 > 사용자 > 기술 > 기기 카테고리(데스크톱/모바일).
- **매일 오전 9시 방문자 PPT 보고** 스케줄 `daily-visitor-report-ppt` 가동 중(집 PC 스케줄).
- ⚠️ 새 HTML 페이지 만들 땐 이 gtag 스니펫을 head에 꼭 넣을 것.

### 2. 화물운송 자격증 모의시험 — 신규 기능
- 페이지 `license-exam.html`, 문제은행 `license_questions.json`(4과목 **160문항**, 해설 포함, 자체제작).
- **비회원도 응시 가능**(로그인 불필요, 비회원엔 가입 권유만). 실전 80문항·80분 + 과목별 연습, 자동채점·합격(48문항)판정·오답노트.
- 메뉴: 헤더 "정보▾" + 모바일 nav에 "📝 자격증 모의시험". 메인에 배너도 있음.
- 문항 더 늘리려면 `license_questions.json`의 questions에 추가만 하면 됨(실전은 랜덤 출제).

### 3. 메인 배너 — 해광툰·모의시험
- index.html `차종별 채용공고` 위에 `.feature-banner`(보라=해광툰, 초록=모의시험) 컬러 카드 2개.

### 4. 공고 수정·삭제가 모든 기기에 반영되도록 수정 ★중요
- 이전엔 관리자가 수정/삭제해도 본인 브라우저(localStorage)만 반영됐음 → **이제 Firebase에 직접 반영**.
- 코드: 공고 카드에 `dataset.docid`(Firestore 문서ID) 저장 → 수정=`db.collection('jobs').doc(docid).update()`, 삭제=`.delete()` → onSnapshot이 전 기기 실시간 반영.
- **Firebase 보안규칙 변경**: jobs `allow update, delete: if true;` (콘솔에서 게시함).
- ⚠️ 보안 트레이드오프: 인증 없이 누구나 이론상 수정/삭제 가능(소규모라 감수). 추후 Firebase Auth로 강화 가능.
- 정적 공고(index.html 하드코딩 견본 ~18개, docid 없음)는 코드 관리 대상.

### 5. 검색엔진 노출 — 4대 검색엔진 커버
- 구글: 서치콘솔 색인요청 완료. 빙·네이버: IndexNow 자동(배포 시).
- **다음(Daum)·네이트 신규 등록** — 별도 저장소 `whdcks498/whdcks498.github.io`의 `robots.txt`에 다음 인증 hash + sitemap 기입해 인증 완료. webmaster.daum.net에서 관리(PIN은 집 PC 메모리에 보관 — 필요 시 대표님께 요청).
- sitemap.xml lastmod 전체 2026-06-16 갱신, license-exam.html 추가.

### 6. 기타
- index.html의 "[자동등록 테스트]" 견본 공고 + 클라우드 테스트공고("5톤 화물기사 테스트") 삭제.
- 회원가입·로그인은 이미 구글시트(Apps Script) 서버 기반 = 멀티기기 정상.

## 회사 코워크가 이어서 할 수 있는 것
- 공고 스팸/허위글 점검(이제 관리자 UI 또는 Firebase 콘솔에서 삭제 시 전 기기 반영).
- 모의시험 문항 확충(license_questions.json).
- 채용글·SEO글·유가 자동갱신(기존 GitHub Actions 유지).
- 홈페이지 변경 배포 시 검색엔진 통보는 자동(IndexNow)+필요시 구글 색인요청.

## 배포·동기화 규칙(불변)
- 배포 = GitHub 웹 업로드(`/upload/main`). 배포 전 반드시 `git fetch + reset --hard origin/main`로 최신화(집↔회사 충돌 방지).
- ⚠️ Chrome 자동번역 켜지면 업로드 화면 파일명이 한글로 보이나 실제는 영문(걱정 말 것).
- 공유파일(index/sitemap/article 등)은 집에서 그냥 푸시 금지 — 항상 동기화 후.
